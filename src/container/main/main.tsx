import Sidebar from "@/components/sidebar/sidebar";

const Main = () => {
  return (
    <main className="flex items-center w-full h-screen">
      <Sidebar />
    </main>
  );
};

export default Main;
